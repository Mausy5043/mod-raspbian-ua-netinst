# Useful OS extensions
packages="apt-utils,logrotate,nfs-common,raspi-copies-and-fills,rsync"

# Support for `raspdiagd`
packages="$packages,python,git,gnuplot,mysql-client,python-mysqldb"

# Handy utilities
packages="$packages,bc,dieharder,htop,lsof,nano,screen,stress,tree"

# My defaults
cmdline="dwc_otg.lpm_enable=0 console=tty1 noatime loglevel=2 cgroup_enable=memory elevator=noop"
disable_root=1
gpu_mem=16
hostname=raspberrypi
timeserver=2.pool.ntp.org
timezone=Europe/Amsterdam
username=pi
userpw=raspberry
user_is_admin=1
locales="en_US.UTF-8"
system_default_locale="en_US.UTF-8"

boot_volume_label=$hostname
